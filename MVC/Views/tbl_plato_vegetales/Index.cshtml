@model IEnumerable<MVC.Models.tbl_plato_vegetales>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomplatov)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.preciouv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_reseta.nomplato)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomplatov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.preciouv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_reseta.nomplato)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idv }) |
            @Html.ActionLink("Details", "Details", new { id=item.idv }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idv })
        </td>
    </tr>
}

</table>
