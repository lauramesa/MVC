@model IEnumerable<MVC.Models.tbl_plato_postre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomplatop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precioup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_reseta.nomplato)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomplatop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precioup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_reseta.nomplato)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idp }) |
            @Html.ActionLink("Details", "Details", new { id=item.idp }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idp })
        </td>
    </tr>
}

</table>
