@model IEnumerable<MVC.Models.tbl_ing_principal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ingrp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ingrp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iding }) |
            @Html.ActionLink("Details", "Details", new { id=item.iding }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iding })
        </td>
    </tr>
}

</table>
