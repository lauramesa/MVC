@model IEnumerable<MVC.Models.tbl_inf_original>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fuente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ubicacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_reseta.nomplato)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fuente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ubicacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_reseta.nomplato)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idoc }) |
            @Html.ActionLink("Details", "Details", new { id=item.idoc }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idoc })
        </td>
    </tr>
}

</table>
